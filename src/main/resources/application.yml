spring:
  application:
    name: GraoMestre
  jpa:
    hibernate:
      ddl-auto: update # Use 'update' for development to automatically create/update tables
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/gm_db?createDatabaseIfNotExist=true # Connect to gm_db, create if it doesn't exist
    username: root
    password: Havan2015@
  mail:
    host: smtp.gmail.com
    port: 587
    username: lucasliaw20@gmail.com
    password: iitw egju pycb vgyc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com

jwt:
  public:
    key: classpath:certs/authz.pub
  private:
    key: classpath:certs/authz.pem

image:
  upload:
    dir: src/main/resources/static/images
  base-path: /images/
app:
  base-url: localhost:8081


server:
  port: 8081
