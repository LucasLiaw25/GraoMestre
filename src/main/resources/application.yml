spring:
  application:
    name: GraoMestre
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:/data/GM_DB
    username: lucas
    password: 123
  h2:
    console:
      enabled: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: lucasliaw20@gmail.com
    password: hurv rsgu nstn eaf
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com

jwt:
  public:
    key: classpath:certs/authz.pub
  private:
    key: classpath:certs/authz.pem

image:
  upload:
    dir: src/main/resources/static/images

app:
  base-url: localhost:8080
